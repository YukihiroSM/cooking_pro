import { UserPossibleMealsPageComponent } from '../pages';

const page = {
  element: UserPossibleMealsPageComponent,
  path: 'USER_POSSIBLE_MEALS',
  protectedRoute: true,
  key: 'user-possible-meals',
};

export default page;
