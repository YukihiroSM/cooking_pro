import { UserProfilePageComponent } from '../components/user-profile.component';

const page = {
  element: UserProfilePageComponent,
  path: 'USER_PROFILE',
  protectedRoute: true,
  key: 'user-profile',
};

export default page;
