import { UserIngredientsPageComponent } from '../pages';

const page = {
  element: UserIngredientsPageComponent,
  path: 'USER_INGREDIENTS',
  protectedRoute: false,
  key: 'user-ingredients',
};

export default page;
