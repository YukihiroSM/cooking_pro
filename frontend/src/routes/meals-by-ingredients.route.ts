import { MealsByIngredientsPageComponent } from '../pages';

const page = {
  element: MealsByIngredientsPageComponent,
  path: 'MEALS_BY_INGREDIENTS',
  protectedRoute: false,
  key: 'meals-by-ingredients',
};

export default page;
