import { FilteredMealsPageComponent } from '../pages';

const page = {
  element: FilteredMealsPageComponent,
  path: 'MEALS_BY_FILTER',
  protectedRoute: false,
  key: 'meals-filtered',
};

export default page;
