import { MealsPageComponent } from '../components/meals.component';

const page = {
  element: MealsPageComponent,
  path: 'MEALS_BY_FILTER',
  protectedRoute: true,
  key: 'meals',
};

export default page;
